# Built-in skills

Each built-in skill is a local, readable workflow package. The desktop UI
exposes these packages through Settings > Agent and invokes them from chat with
`@skill-name`. They do not contain credentials or remote code.
